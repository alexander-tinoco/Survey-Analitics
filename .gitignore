# Environment & secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Django
*.sqlite3
media/
staticfiles/

# Testing & coverage
.pytest_cache/
.coverage
coverage.xml
htmlcov/

# Tooling
.ruff_cache/
.mypy_cache/

# OS & editors
.DS_Store
.idea/
.vscode/

# Agent tooling. Reinstallable with `npx impeccable install`, so the skill's
# own files are not project source. PRODUCT.md and DESIGN.md are, and are
# committed at the repository root.
.claude/skills/
.claude/settings.local.json
.claude/RESUME.md
.impeccable/
